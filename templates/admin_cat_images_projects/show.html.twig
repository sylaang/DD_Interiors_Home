{% extends 'base_admin.html.twig' %}

{% block title %}CatImagesProjects{% endblock %}

{% block body %}
    <h1>CatImagesProjects</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_images_project.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cat_images_project.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_cat_images_projects_index') }}">back to list</a>

    <a href="{{ path('app_admin_cat_images_projects_edit', {'id': cat_images_project.id}) }}">edit</a>

    {{ include('admin_cat_images_projects/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('app_admin_categories_prix_delete', {'id': categories_prix.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categories_prix.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_admin_forfaits_delete', {'id': forfait.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forfait.id) }}">
    <button class="btn">Delete</button>
</form>

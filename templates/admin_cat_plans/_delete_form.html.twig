<form method="post" action="{{ path('app_admin_cat_plans_delete', {'id': cat_images_project.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cat_plan.id) }}">
    <button class="btn">Delete</button>
</form>

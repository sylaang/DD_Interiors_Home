{% extends 'base_admin.html.twig' %}

{% block title %}CatPlans{% endblock %}

{% block body %}
    <h1>CatPlans</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_plan.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cat_plan.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_cat_plans_index') }}">back to list</a>

    <a href="{{ path('app_admin_cat_plans_edit', {'id': cat_plan.id}) }}">edit</a>

    {{ include('admin_cat_plans/_delete_form.html.twig') }}
{% endblock %}
